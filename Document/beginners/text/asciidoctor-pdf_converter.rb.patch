diff -c /Users/kuboaki/.rbenv/versions/2.3.0/lib/ruby/gems/2.3.0/gems/asciidoctor-pdf-1.5.0.alpha.15/lib/asciidoctor-pdf/converter.rb\~ /Users/kuboaki/.rbenv/versions/2.3.0/lib/ruby/gems/2.3.0/gems/asciidoctor-pdf-1.5.0.alpha.15/lib/asciidoctor-pdf/converter.rb
*** /Users/kuboaki/.rbenv/versions/2.3.0/lib/ruby/gems/2.3.0/gems/asciidoctor-pdf-1.5.0.alpha.15/lib/asciidoctor-pdf/converter.rb~	Mon Jul 31 12:22:14 2017
--- /Users/kuboaki/.rbenv/versions/2.3.0/lib/ruby/gems/2.3.0/gems/asciidoctor-pdf-1.5.0.alpha.15/lib/asciidoctor-pdf/converter.rb	Wed Aug 16 13:44:48 2017
***************
*** 1,4 ****
! # encoding: UTF-8
  # TODO cleanup imports...decide what belongs in asciidoctor-pdf.rb
  require 'prawn'
  require_relative 'ttfunk_ext'
--- 1,4 ----
! # encoding: utf-8
  # TODO cleanup imports...decide what belongs in asciidoctor-pdf.rb
  require 'prawn'
  require_relative 'ttfunk_ext'
***************
*** 825,831 ****
      @list_numbers ||= []
      # FIXME move \u2460 to constant (or theme setting)
      # \u2460 = circled one, \u24f5 = double circled one, \u278b = negative circled one
!     @list_numbers << %(\u2460)
      #stroke_horizontal_rule @theme.caption_border_bottom_color
      line_metrics = calc_line_metrics @theme.base_line_height
      node.items.each_with_index do |item, idx|
--- 825,831 ----
      @list_numbers ||= []
      # FIXME move \u2460 to constant (or theme setting)
      # \u2460 = circled one, \u24f5 = double circled one, \u278b = negative circled one
!     @list_numbers <<  %(\u2776)  # %(\u2460)
      #stroke_horizontal_rule @theme.caption_border_bottom_color
      line_metrics = calc_line_metrics @theme.base_line_height
      node.items.each_with_index do |item, idx|
***************
*** 926,932 ****
            nil
          else
            if Bullets.key?(candidate = style.to_sym)
!             candidate 
            else
              warn %(asciidoctor: WARNING: unknown unordered list style: #{candidate})
              :disc
--- 926,932 ----
            nil
          else
            if Bullets.key?(candidate = style.to_sym)
!             candidate
            else
              warn %(asciidoctor: WARNING: unknown unordered list style: #{candidate})
              :disc
***************
*** 1471,1477 ****
      # FIXME make starting glyph a constant and/or theme setting
      # FIXME use lookup table for glyphs instead of relying on counting
      # \u2460 = circled one, \u24f5 = double circled one, \u278b = negative circled one
!     glyph = %(\u2460)
      (number - 1).times { glyph = glyph.next }
      glyph
    end
--- 1471,1477 ----
      # FIXME make starting glyph a constant and/or theme setting
      # FIXME use lookup table for glyphs instead of relying on counting
      # \u2460 = circled one, \u24f5 = double circled one, \u278b = negative circled one
!     glyph = %(\u2776)  # %(\u2460)
      (number - 1).times { glyph = glyph.next }
      glyph
    end

Diff finished.  Wed Aug 16 15:11:03 2017
